import React from "react";
import Workspace from "@layouts/Workspace";


const Channel = () => {
    return (
        <Workspace>
            <div>로그인한것을 축하한다.</div>
        </Workspace>
    )
}

export default Channel;